{
  "parameters" : [
    {"name" : "api.security.whitelist",
      "value" : ["127.0.0.1","::1"],
      "consumer" : ["all"],
      "description" : "api endpoint for private server to server whitelist"
    },
    {"name" : "api.security.blacklist",
      "value" : [""],
      "consumer" : ["all"],
      "description" : "api endpoint for public to server blacklist"
    },
    {"name" : "api.security.ratelimit.requests",
      "value" : 30,
      "consumer" : ["all"],
      "description" : "rate limiter max requests in x seconds defined by api.security.ratelimit.lifetime.secs"
    },
    {"name" : "api.security.ratelimit.lifetime.secs",
      "value" : 60,
      "consumer" : ["all"],
      "description" : "rate limiter api.security.ratelimit.requests in x seconds"
    },
    {"name" : "token.refresh.secret",
      "value" : "S^F2=gqT_$72zT3U*g8Jo3w#VCWQ;J",
      "consumer" : ["auth"],
      "description" : "refresh token secret key"
    },
    {"name" : "token.refresh.expires",
      "value" : 28800,
      "consumer" : ["auth"],
      "description" : "refresh token lifetime in minutes"
    },
    {"name" : "token.refresh.issuer",
      "value" : "auth",
      "consumer" : ["auth"],
      "description" : "issuer of the refresh token"
    },
    {"name" : "token.refresh.enforce.valid.ip",
      "value" : "true",
      "consumer" : ["auth"],
      "description" : "check the request headers ip against the ip used to create the users last refresh token. If true and no match then token is invalidated"
    },
    {"name" : "token.auth.secret",
      "value" : "Qu1Ozg#3ajyv3Fxl3bM6U)+28R8H.1",
      "consumer" : ["all"],
      "description" : "auth token secret key"
    },
    {"name" : "token.auth.expires",
      "value" : 60,
      "consumer" : ["auth"],
      "description" : "auth token lifetime in minutes"
    },
    {"name" : "token.auth.issuer",
      "value" : "auth",
      "consumer" : ["all"],
      "description" : "issuer of the auth token"
    }
  ]
}